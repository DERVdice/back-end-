ƒл€ проверки запустить cmd или powershell в текущей директории и выполнить следующие комманды:

1. «агрузка image в docker из .tar архива:
docker load < derv-dice.latest.tar

2. «апуск контейнера с именем travolta на 8080 порте. ≈сли 8080 зан€т, то заменить первое число на свободный порт
docker run --name travolta -i -t -p 8080:8080 derv-dice:latest

3. «апуск веб-сервера в контейнере в фоновом режиме
go run WebServer.go &

(¬се, что он умеет делать - отправл€ть домашнюю страницу index.html по адресу "/" или travolta.html по адресу "/travolta"

4. ќткрыть браузер по адресу http://192.168.99.100:8080/ и увидеть (€ надеюсь) index.html

” мен€ все работает именно по этому адресу т.к. € "счастливый обладатель" windows 10 home, котора€ не поддерживает 
docker desktop, поэтому € пользовалс€ docker toolbox, работающей на основе oracle virtualbox, котора€ в свою очередь,
дает именно такой стандартный ip docker'у

≈сли по адресу ничего нет, то проверить localhost:8080

≈сли не работает и он, то нужно узнать <docker-machine ip default>, вписав эту комманду в cmd, затем 
проверить его на порту 8080



Ќадеюсь, все будет ок.
derv-dice@mail.ru
 
